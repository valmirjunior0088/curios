//! Erasure, end to end: the typing facts it rests on, what it deletes, that a deleted proof gives a program no behaviour, and that what it keeps computes the value the checker proved.

mod dropped_tests;
mod evaluation_tests;
mod irrelevance_tests;
mod value_tests;
