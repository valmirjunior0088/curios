-- The data structures and codecs: the crit-bit map and set, arbitrary-precision numbers, the path algebra, the format-string slots, and HTTP's vocabulary.
mod map;

mod big_num;

mod path;

mod fmt;

mod http;
