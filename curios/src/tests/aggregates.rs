//! `Bin` and `List` as free monoids, their spreads, the packed forms, and the `Option`, `Result` and `Vec` surfaces, end to end.

mod monoid_tests;
mod option_result_tests;
mod packed_tests;
mod spread_tests;
mod vec_tests;
