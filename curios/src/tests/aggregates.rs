//! `Bin` and `List` as free monoids, their spreads, the packed forms, and the `List`, `Option`, `Result` and `Vec` surfaces, end to end.

mod list_tests;
mod monoid_tests;
mod option_result_tests;
mod packed_tests;
mod spread_tests;
mod vec_tests;
