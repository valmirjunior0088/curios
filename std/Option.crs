pub union Option(A : Type)
| some(A)
| none()
end

pub use Option/*;

pub let bind(@A : Type, @B : Type) -> (Option(A), A -> Option(B)) -> Option(B) =
    (m, f) =>
        match m
        | some(a) => f(a)
        | none() => Option/none()
        end;
