[package]
name = "curios-binaryen"
version.workspace = true
edition.workspace = true
description = "Wasm optimization for curios via the vendored Binaryen library"

[dependencies]

[build-dependencies]
cmake = { version = "0.1.54" }
