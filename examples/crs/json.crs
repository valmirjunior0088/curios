pub mod Value;
pub mod encode;
pub mod decode;
use /Parse;
let Parse(T: Type) -> Type = Parse/Parse(T);

pub let Value : Type = Value/Value;
pub let encode : Value -> Bin = encode/encode;
pub let decode : Parse(Value) = decode/decode;
