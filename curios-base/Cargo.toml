[package]
name = "curios-base"
version.workspace = true
edition.workspace = true
description = "Foundational utilities shared across the curios pipeline: spans, entropy, the name! macro, parser/printer monads, and the slice suffix-view laws"
