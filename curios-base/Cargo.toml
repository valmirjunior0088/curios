[package]
name = "curios-base"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Foundational utilities shared across every Curios stage: spans, names, entropy, parser/printer monads, and packed values"

[lints]
workspace = true

[features]
archive = ["dep:rkyv"]

[dependencies]
num-bigint = { workspace = true }
num-traits = { workspace = true }
rkyv = { workspace = true, optional = true }
