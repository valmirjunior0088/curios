# A pure program rebuilds what an impure one would mutate

**Decision.** Death-birth churn — a construction beside a co-resident dying value of the same shape, which an impure program would have mutated in place — is delegated to the host collector. The compiler carries no reuse machinery: no reference counts, no sharing flags, no regions, no in-place update of a uniquely owned object. What the native half does about the cost is a runtime decision, [`curios-runtime/README.md`](../../../curios-runtime/README.md)'s "The heap is sized ahead of its churn", and what remains open is [The survivors are what cost](../../roadmap/collector-economics-spec.md)'s.

**Rationale.** Delegating churn to the host collector is the WasmGC mainstream's strategy — V8's porting guidance, dart2wasm and Kotlin/Wasm shipping no reuse machinery — and the browser half of this toolchain already runs under V8's generational collector. On the native half, both churn-class workloads measured about two thirds of their churn in the collector recopying survivors, which sizing the semi-space removes, and one third in the birth path itself, which is what the compiler-side levers below were priced against and declined on.

**Rejected — the compiler-side levers, on the census's numbers.** The death-birth census (`death_birth_census`, beside the aggregate-flow census) found the population pervasive — 509 of 1 168 constructions beside a co-resident dying value of matching width — and entirely cross-frame: the dying value always arrives as a parameter, so no intra-function reuse mechanism reaches it. Static array-scoped reuse was refused because the linearly threaded builder population it targets (136 of 256 rope extends) is not copy-bound — the rope already amortizes a builder to one node per extend and a single force-time fill; reinstate on observed repeated force-fills, the alternating append-and-read hazard the rope documents. Construction coalescing and constructor-tail rewrites were refused because their shapes measure at zero — no constructed-width pairs exist corpus-wide, the walks are accumulator-first, and a tail-modulo-cons write needs the mutable tuple field the representation deliberately lacks; reinstate on either count rising from zero.

**Rejected — reuse machinery as such.** Recorded so they are not relitigated. A per-object reference count on an engine-owned heap frees nothing and is purchased solely as a uniqueness test: dup/drop traffic on every duplication, payload fields losing immutability, and an undercount silently corrupting a shared value, a permanent discipline on every future `curios-cont` pass. A sticky one-bit sharing flag shares the mut-ification and the misplaced-dup soundness class. Regions and arenas are inexpressible on WasmGC, where nothing frees wholesale. Owning allocation in linear memory is a different compilation strategy, forfeiting the engine collector and the browser story to buy the Perceus precondition. Perceus stays the comparator, never the mechanism.
