# `Task` sleep and timeout

Not refined yet. This placeholder reserves the specification location for the remaining `/std/Task` async combinators until they are refined into a working implementation specification.

## Covered roadmap items

- `sleep`/`timeout` for `/std/Task`

## Known constraints

- Builds on the landed concurrent `Task` combinators and the clock host operations already exposed through `Io`.
