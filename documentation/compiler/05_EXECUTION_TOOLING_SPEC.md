# Execution tooling

Not refined yet. This umbrella placeholder reserves the specification location for the roadmap items below until each is refined into a working implementation specification.

## Covered roadmap items

- Terminal REPL
- Test runner

## Known constraints

- Both tools drive compilation and execution through the native driver and runtime rather than analyzing source; the bootstrap testing strategy already anticipates the self-hosted test runner ([bootstrap contracts](../bootstrap/01_CONTRACTS_SPEC.md)).
