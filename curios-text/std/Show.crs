use /std/{Nat, Int, Flt, Bln, Str};

pub concept Show(A : Type) : Type {
    show(A) -> Str
}

pub use Show/*;

satisfy Show(Nat) {
    show(n) = Nat/to_str(n)
}

satisfy Show(Int) {
    show(n) = Int/to_str(n)
}

satisfy Show(Flt) {
    show(f) = Flt/to_str(f)
}

satisfy Show(Bln) {
    show(b) = Bln/to_str(b)
}

satisfy Show(Str) {
    show(s) = s
}
