pub use /sys/Io/{args};
use /std/{Bytes, Nat, Str, Option, False};

pub let env(name : Str) -> Option(Bytes) =
    let r = /sys/Io/env(Str/to_bytes(name));
    match r.status == /sys/Io/Status/ok
    | true => Option/some(r.value)
    | false => Option/none()
    end;

pub let exit(code : Nat) -> False =
    /sys/Io/exit(code);
