name: Release

on:
  push:
    tags:
      - "release/*"

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always

jobs:
  native:
    name: Native binary (${{ matrix.target }})
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 60

    strategy:
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            runner: ubuntu-24.04
            asset: curios-linux-x86_64
          - target: aarch64-unknown-linux-gnu
            runner: ubuntu-24.04-arm
            asset: curios-linux-aarch64
          - target: aarch64-apple-darwin
            runner: macos-14
            asset: curios-macos-aarch64

    steps:
      - uses: actions/checkout@v6

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}
          cache-directories: |
            curios/.artifacts
            curios-binaryen/.artifacts

      - name: Build
        run: cargo xtask build

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.asset }}
          path: target/release/curios
          retention-days: 1

  wasm:
    name: Browser bundle
    runs-on: ubuntu-24.04
    timeout-minutes: 60

    steps:
      - uses: actions/checkout@v6

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      - uses: Swatinem/rust-cache@v2
        with:
          key: wasm32-unknown-unknown

      - name: Build curios-js
        run: cargo xtask js

      - name: Archive package
        run: mkdir -p .artifacts && tar -czf .artifacts/curios-js.tar.gz -C curios-js/.artifacts/wasm32-unknown-unknown .

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: curios-js
          path: .artifacts/curios-js.tar.gz
          retention-days: 1

  docs:
    name: Documentation
    runs-on: ubuntu-24.04
    timeout-minutes: 60

    steps:
      - uses: actions/checkout@v6

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2
        with:
          key: docs
          cache-directories: |
            curios/.artifacts
            curios-binaryen/.artifacts

      - name: Build runtime launcher
        run: cargo xtask runtime

      - name: Build docs
        run: cargo doc --workspace --no-deps

      - name: Add docs root redirect
        run: echo '<!DOCTYPE html><meta http-equiv="refresh" content="0; url=curios/index.html">' > target/doc/index.html

      - name: Archive docs
        run: mkdir -p .artifacts && tar -czf .artifacts/curios-docs.tar.gz -C target/doc .

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: curios-docs
          path: .artifacts/curios-docs.tar.gz
          retention-days: 1

  release:
    name: GitHub release
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    needs: [native, wasm, docs]

    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v6

      - uses: actions/download-artifact@v7
        with:
          path: .artifacts/downloads

      - name: Rename artifacts
        run: |
          mv .artifacts/downloads/curios-linux-x86_64/curios .artifacts/curios-linux-x86_64
          mv .artifacts/downloads/curios-linux-aarch64/curios .artifacts/curios-linux-aarch64
          mv .artifacts/downloads/curios-macos-aarch64/curios .artifacts/curios-macos-aarch64
          mv .artifacts/downloads/curios-js/curios-js.tar.gz .artifacts/curios-js.tar.gz
          mv .artifacts/downloads/curios-docs/curios-docs.tar.gz .artifacts/curios-docs.tar.gz

      # The installer's version is baked in here rather than discovered at run time, so the script a release ships installs that release's binary and a pinned URL stays pinned. The template is rendered to a fresh file rather than edited in place: what ships is a build product, never a mutated checkout.
      - name: Prepare installer
        run: |
          (cd .artifacts && sha256sum curios-linux-x86_64 curios-linux-aarch64 curios-macos-aarch64 > checksums.txt)
          sed "s/@VERSION@/${GITHUB_REF_NAME#release/}/" .github/workflows/install.sh.in > .artifacts/install.sh

      - name: Create release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create ${{ github.ref_name }} \
            --title "${{ github.ref_name }}" \
            --generate-notes \
            .artifacts/curios-linux-x86_64 \
            .artifacts/curios-linux-aarch64 \
            .artifacts/curios-macos-aarch64 \
            .artifacts/curios-js.tar.gz \
            .artifacts/curios-docs.tar.gz \
            .artifacts/checksums.txt \
            .artifacts/install.sh
