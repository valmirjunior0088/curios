use /std/{Nat, Str};
use /std/Tui/{Style, Frame, text};

-- | `s` wrapped at `w`, each line fitted, the whole fitted to `h`.
pub let draw(style: Style, s: Str, w: Nat, h: Nat) -> Frame(w, h) =
    Frame/of_lines(style, text/wrap(s, w), w, h);
