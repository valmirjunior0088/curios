use /std/{Bool, Flt, Str, List};

-- | A JSON value.
pub induct Json: pub Type
| null()
| bool(Bool)
| num(Flt)
| str(Str)
| arr(List(Json))
| obj(List({Str, Json}))
end

pub use Json/*;

mod encode;
pub use encode/{encode};

mod decode;
pub use decode/{decode};
