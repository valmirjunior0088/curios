use /std/{Ordering};
use /std/ops/{Eql};

-- | A total order: `ord` decides `lt`, `eq` or `gt`, and `Eql` is its superclass.
pub concept Ord(A: Type): pub Type {
    use Eql(A),
    ord(A, A) -> Ordering,
}

pub use Ord/*;
