-- | The proposition with no proof, which anything may be concluded from.
pub induct False: pub Prop
end

-- | The refutation of `P`.
-- |
-- | A value of it turns any `P` into a contradiction, so a type with one is uninhabited.
pub let Not(P: Type) -> Type =
    (P) -> False;
