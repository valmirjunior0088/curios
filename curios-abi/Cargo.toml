[package]
name = "curios-abi"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "The host/guest wire contract shared by the Curios compiler and runtimes: ABI constants and self-describing foreign-function rows"

[lints]
workspace = true

[features]
archive = ["dep:rkyv"]

[dependencies]
num-bigint = { workspace = true }
rkyv = { workspace = true, optional = true }
