pub use /syn/{Add};
pub use /syn/Add/{add};
use /std/{Nat, Int, Flt, BigInt};

satisfy Add(Nat) {
    add(a, b) = Nat/add(a, b)
}

satisfy Add(Int) {
    add(a, b) = Int/add(a, b)
}

satisfy Add(Flt) {
    add(a, b) = Flt/add(a, b)
}

satisfy Add(BigInt) {
    add(a, b) = BigInt/add(a, b)
}
